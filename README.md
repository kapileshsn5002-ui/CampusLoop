# CampusLoop

CampusLoop is a campus management application containing both backend and frontend services.

## Project Structure

- **`frontend/`**: React + Vite frontend UI application
- **`backend/`**: Java Spring Boot backend service
- **`database/`**: Database schemas and migration scripts
- **`tests/`**: End-to-end integration and API tests
